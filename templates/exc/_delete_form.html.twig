<form method="post" action="{{ path('exc_delete', {'id': exc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exc.id) }}">
    <button class="btn">Delete</button>
</form>
