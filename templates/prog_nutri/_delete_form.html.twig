<form method="post" action="{{ path('prog_nutri_delete', {'id': prog_nutri.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prog_nutri.id) }}">
    <button class="btn">Delete</button>
</form>
